let ctx:AudioContext|undefined;
export function sound(kind:'click'|'wrong'|'hint'|'win',enabled:boolean){if(!enabled)return;try{ctx??=new AudioContext();void ctx.resume();const freqs=kind==='win'?[523,659,784,1046]:kind==='wrong'?[180,135]:kind==='hint'?[440,660]:[520];freqs.forEach((f,i)=>{const o=ctx!.createOscillator(),g=ctx!.createGain(),t=ctx!.currentTime+i*.09;o.type=kind==='wrong'?'triangle':'sine';o.frequency.value=f;g.gain.setValueAtTime(0,t);g.gain.linearRampToValueAtTime(.055,t+.012);g.gain.exponentialRampToValueAtTime(.001,t+.16);o.connect(g);g.connect(ctx!.destination);o.start(t);o.stop(t+.2);});if(kind==='win'){const b=ctx.createBuffer(1,ctx.sampleRate,ctx.sampleRate),v=b.getChannelData(0);for(let i=0;i<v.length;i++)v[i]=(Math.random()*2-1)*.07*(1-i/v.length);const n=ctx.createBufferSource(),f=ctx.createBiquadFilter();f.type='lowpass';f.frequency.value=900;n.buffer=b;n.connect(f);f.connect(ctx.destination);n.start();}}catch{}}
